<html>
<head>
    <title>Level Idea Generator</title>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans" rel="stylesheet">
    <style>
        {{ styles|raw }}
    </style>
</head>
<body>
    <div id="container">
        <div class="top"><h1>I am bad at being creative</h1></div>
        <div class="top"><h2>So here is a level idea generated by a dice roll:</h2></div>
        <div id="idea-container">
            {% for idea in ideas %}
                {% if loop.index0 > 0 %}
                    <div class="line" id="plus">&plus;</div>
                {% endif %}
                <strong class="line idea">{{ idea }}</strong>
            {% endfor %}
        </div>
        <div class="bottom">
            {% if ideas|length == 2 %}
                <a href="?">Again?</a>
                <a href="?count=3">More?</a>
            {% else %}
                <a href="?count={{ ideas|length }}">Again?</a>
                <a href="?">Less?</a>
            {% endif %}
        </div>
    </div>
</body>
</html>